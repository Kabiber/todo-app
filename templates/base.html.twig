<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Todo App{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Стили для фонового изображения */
        body {
            margin: 0;
            padding: 0;
            background: url('{{ asset('images/sunset.jpg') }}') no-repeat center center fixed;
            background-size: cover; /* Масштабирует изображение под размер экрана */
            height: 100vh; /* Занимает всю высоту viewport */
        }

        /* Стили для навигационной строки */
        .navbar {
            background-color: rgba(0, 0, 0, 0.7); /* Полупрозрачный чёрный фон */
            backdrop-filter: blur(10px); /* Добавляет размытие для лучшей видимости */
        }

        .navbar-brand, .nav-link {
            color: white !important; /* Белый текст для контраста */
        }

        /* Стили для контейнера списка задач */
        .task-list-container {
            background: rgba(255, 255, 255, 0.8); /* Полупрозрачный белый фон */
            border-radius: 10px; /* Закруглённые углы */
            padding: 20px; /* Отступы внутри контейнера */
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Тень для лучшего выделения */
            max-width: 800px; /* Ограничиваем ширину контейнера */
            margin: 50px auto; /* Центрируем контейнер по горизонтали */
        }

        /* Стили для отдельных задач */
        .task-item {
            background: rgba(255, 255, 255, 0.9); /* Более светлый фон для каждой задачи */
            border: 1px solid #ddd; /* Лёгкая граница */
            margin-bottom: 10px; /* Пробел между задачами */
            padding: 15px; /* Отступы внутри элемента задачи */
            border-radius: 5px; /* Закруглённые углы */
        }

        .task-item.completed {
            background: rgba(212, 237, 218, 0.9); /* Зеленоватый фон для выполненных задач */
            text-decoration: line-through; /* Подчёркивание для выполненных задач */
        }

        .container {
            position: relative;
            z-index: 1; /* Чтобы содержимое было поверх фона */
        }
    </style>
</head>
<body>
<!-- Навигационная строка -->
<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('task_index') }}">Ежедневник</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('task_create') }}">Создать задачу</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<!-- Основной контент -->
<div class="container mt-5">
    <div class="task-list-container">
        {% block body %}{% endblock %}
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>